# AdSense authorised-sellers file.
#
# Required before ads will serve: Google checks https://<domain>/ads.txt and, if
# it is missing or doesn't name your publisher ID, treats the inventory as
# unauthorised. Uncomment the line below and replace pub-0000000000000000 with
# your own publisher ID (the digits in ca-pub-…, no "ca-" prefix here).
#
# google.com, pub-0000000000000000, DIRECT, f08c47fec0942fa0
#
# Note: this file does nothing on a *.netlify.app subdomain, and neither does
# anything else you try — ads.txt is read per registrable domain, and AdSense
# verifies ownership at the parent domain (netlify.app), which belongs to Netlify.
# A domain of your own is a prerequisite for serving ads at all, not an upgrade.
